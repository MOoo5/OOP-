### ✅ الأول: يعني إيه Friend Function؟

ببساطة كدا:

هي **function** بنخليها "صاحبة" لكلاس معين، بحيث تقدر تدخل على **الـ private والـ protected data members** جوه الكلاس ده، بالرغم من إنها مش عضو (member) في الكلاس.

---

### 📌 الشكل العام:

```cpp
class MyClass {
private:
    int secret;

public:
    MyClass() { secret = 42; }

    // دي بقت صاحبة الكلاس
    friend void showSecret(MyClass obj);
};

void showSecret(MyClass obj) {
    cout << "Secret is: " << obj.secret << endl;  // مسموحلها تدخل
}

```

### ✅ ملحوظة:

- `friend` function مش لازم تكون جوه الكلاس، بس لازم يتكتب اسمها جواه بالكلمة المفتاحية `friend`.

---

### ✅ ثانيًا: يعني إيه Friend Class؟

دي بقى لما بنخلي **كلاس كامل** صاحب لكلاس تاني، يعني كل الـ functions اللي جوا الكلاس الصاحب يقدروا يدخلوا على الحاجات الخاصة (private/protected) بتاع الكلاس التاني.

---

### 📌 الشكل العام:

```cpp
class B;  // Forward declaration

class A {
private:
    int x;

public:
    A() { x = 100; }

    // بنخلي B صاحب A
    friend class B;
};

class B {
public:
    void printA(A obj) {
        cout << "Value of x: " << obj.x << endl;
    }
};

```

### ✅ ملحوظة:

- لما بنكتب `friend class B;` جوا `class A`، كل الفانكشنات بتاعة `B` تقدر تدخل على `A`.

---

### 💡 الفرق بين Friend Function و Friend Class:

| Friend Function | Friend Class |
| --- | --- |
| Function واحدة بس تبقى صاحبة | كلاس كامل يبقى صاحب |
| بنكتب `friend` قبل اسم الفانكشن | بنكتب `friend class B;` |
| ممكن تبقى global أو member | لازم تكون class كاملة |

---

### ❌ ليه ما نستخدمش الـ friend كتير؟

عشان بتكسر مبدأ مهم في OOP اسمه **Encapsulation**، المفروض الكلاس يحمي بياناته، لكن لما تفتحله الباب بـ `friend` بتكشف كل حاجة.

---

### 🧠 استخدامات مفيدة للـ friend:

- لو عايز تعمل **function operator overloading** (زي `<<` أو `>>`) وتدخل على data members خاصة.
- لو كلاس محتاج يدخل بيانات من كلاس تاني بدون ما تبني عليه inheritance.

---

```cpp
#include <iostream>
using namespace std;

// 1. إعلان مسبق عشان كلاس CRectangle يعرف إن في حاجة اسمها CSquare
class CSquare; 

class CRectangle {
    int width, height;
public:
    int area() { return (width * height); }
    // دالة هتحول المربع لمستطيل
    void convert(CSquare); 
};

class CSquare {
private:
    int side; // طول الضلع (private)
public:
    void set_side(int x) { side = x; }
    
    // 2. هنا بنقول إن CRectangle هو صديقنا (يقدر يشوف الـ side)
    friend class CRectangle; 
};

// 3. تعريف الدالة خارج الكلاس باستخدام Scope Resolution ::
void CRectangle::convert(CSquare a) {
    // لاحظ هنا: قدرنا نوصل لـ a.side مباشرة لأننا "أصدقاء"
    width = a.side;
    height = a.side;
}

int main() {
    CSquare sqr;
    CRectangle rect;
    
    sqr.set_side(4);    // ضلع المربع = 4
    rect.convert(sqr);  // حولنا المستطيل ليكون بنفس أبعاد المربع
    
    cout << rect.area() << endl; // النتيجة هتكون 16
    return 0;
}
```

### الـ `friend class CRectangle;`

بمجرد كتابة السطر ده جوه كلاس `CSquare`، إنت بتدي "تصريح أمني" لكلاس `CRectangle` إنه يدخل منطقة الـ `private` عندك ويستخدم متغير `side`.

### 2. دالة الـ `convert`

الدالة دي موجودة جوه `CRectangle`. هي بتاخد كائن من نوع `CSquare` وبتاخد قيمة الـ `side` بتاعته وتحطها في الـ `width` والـ `height` بتوع المستطيل. لولا كلمة `friend` كان السطر ده `width = a.side;` هيدي Error فوراً.

### 3. الترتيب مهم

لازم تعلن عن الـ `friend` جوه الكلاس اللي بياناته "محمية" (المربع في حالتنا).### ✅ الأول: يعني إيه Friend Function؟

ببساطة كدا:

هي **function** بنخليها "صاحبة" لكلاس معين، بحيث تقدر تدخل على **الـ private والـ protected data members** جوه الكلاس ده، بالرغم من إنها مش عضو (member) في الكلاس.

---

### 📌 الشكل العام:

```cpp
class MyClass {
private:
    int secret;

public:
    MyClass() { secret = 42; }

    // دي بقت صاحبة الكلاس
    friend void showSecret(MyClass obj);
};

void showSecret(MyClass obj) {
    cout << "Secret is: " << obj.secret << endl;  // مسموحلها تدخل
}

```

### ✅ ملحوظة:

- `friend` function مش لازم تكون جوه الكلاس، بس لازم يتكتب اسمها جواه بالكلمة المفتاحية `friend`.

---

### ✅ ثانيًا: يعني إيه Friend Class؟

دي بقى لما بنخلي **كلاس كامل** صاحب لكلاس تاني، يعني كل الـ functions اللي جوا الكلاس الصاحب يقدروا يدخلوا على الحاجات الخاصة (private/protected) بتاع الكلاس التاني.

---

### 📌 الشكل العام:

```cpp
class B;  // Forward declaration

class A {
private:
    int x;

public:
    A() { x = 100; }

    // بنخلي B صاحب A
    friend class B;
};

class B {
public:
    void printA(A obj) {
        cout << "Value of x: " << obj.x << endl;
    }
};

```

### ✅ ملحوظة:

- لما بنكتب `friend class B;` جوا `class A`، كل الفانكشنات بتاعة `B` تقدر تدخل على `A`.

---

### 💡 الفرق بين Friend Function و Friend Class:

| Friend Function | Friend Class |
| --- | --- |
| Function واحدة بس تبقى صاحبة | كلاس كامل يبقى صاحب |
| بنكتب `friend` قبل اسم الفانكشن | بنكتب `friend class B;` |
| ممكن تبقى global أو member | لازم تكون class كاملة |

---

### ❌ ليه ما نستخدمش الـ friend كتير؟

عشان بتكسر مبدأ مهم في OOP اسمه **Encapsulation**، المفروض الكلاس يحمي بياناته، لكن لما تفتحله الباب بـ `friend` بتكشف كل حاجة.

---

### 🧠 استخدامات مفيدة للـ friend:

- لو عايز تعمل **function operator overloading** (زي `<<` أو `>>`) وتدخل على data members خاصة.
- لو كلاس محتاج يدخل بيانات من كلاس تاني بدون ما تبني عليه inheritance.

---

```cpp
#include <iostream>
using namespace std;

// 1. إعلان مسبق عشان كلاس CRectangle يعرف إن في حاجة اسمها CSquare
class CSquare; 

class CRectangle {
    int width, height;
public:
    int area() { return (width * height); }
    // دالة هتحول المربع لمستطيل
    void convert(CSquare); 
};

class CSquare {
private:
    int side; // طول الضلع (private)
public:
    void set_side(int x) { side = x; }
    
    // 2. هنا بنقول إن CRectangle هو صديقنا (يقدر يشوف الـ side)
    friend class CRectangle; 
};

// 3. تعريف الدالة خارج الكلاس باستخدام Scope Resolution ::
void CRectangle::convert(CSquare a) {
    // لاحظ هنا: قدرنا نوصل لـ a.side مباشرة لأننا "أصدقاء"
    width = a.side;
    height = a.side;
}

int main() {
    CSquare sqr;
    CRectangle rect;
    
    sqr.set_side(4);    // ضلع المربع = 4
    rect.convert(sqr);  // حولنا المستطيل ليكون بنفس أبعاد المربع
    
    cout << rect.area() << endl; // النتيجة هتكون 16
    return 0;
}
```

### الـ `friend class CRectangle;`

بمجرد كتابة السطر ده جوه كلاس `CSquare`، إنت بتدي "تصريح أمني" لكلاس `CRectangle` إنه يدخل منطقة الـ `private` عندك ويستخدم متغير `side`.

### 2. دالة الـ `convert`

الدالة دي موجودة جوه `CRectangle`. هي بتاخد كائن من نوع `CSquare` وبتاخد قيمة الـ `side` بتاعته وتحطها في الـ `width` والـ `height` بتوع المستطيل. لولا كلمة `friend` كان السطر ده `width = a.side;` هيدي Error فوراً.

### 3. الترتيب مهم

لازم تعلن عن الـ `friend` جوه الكلاس اللي بياناته "محمية" (المربع في حالتنا).### ✅ الأول: يعني إيه Friend Function؟

ببساطة كدا:

هي **function** بنخليها "صاحبة" لكلاس معين، بحيث تقدر تدخل على **الـ private والـ protected data members** جوه الكلاس ده، بالرغم من إنها مش عضو (member) في الكلاس.

---

### 📌 الشكل العام:

```cpp
class MyClass {
private:
    int secret;

public:
    MyClass() { secret = 42; }

    // دي بقت صاحبة الكلاس
    friend void showSecret(MyClass obj);
};

void showSecret(MyClass obj) {
    cout << "Secret is: " << obj.secret << endl;  // مسموحلها تدخل
}

```

### ✅ ملحوظة:

- `friend` function مش لازم تكون جوه الكلاس، بس لازم يتكتب اسمها جواه بالكلمة المفتاحية `friend`.

---

### ✅ ثانيًا: يعني إيه Friend Class؟

دي بقى لما بنخلي **كلاس كامل** صاحب لكلاس تاني، يعني كل الـ functions اللي جوا الكلاس الصاحب يقدروا يدخلوا على الحاجات الخاصة (private/protected) بتاع الكلاس التاني.

---

### 📌 الشكل العام:

```cpp
class B;  // Forward declaration

class A {
private:
    int x;

public:
    A() { x = 100; }

    // بنخلي B صاحب A
    friend class B;
};

class B {
public:
    void printA(A obj) {
        cout << "Value of x: " << obj.x << endl;
    }
};

```

### ✅ ملحوظة:

- لما بنكتب `friend class B;` جوا `class A`، كل الفانكشنات بتاعة `B` تقدر تدخل على `A`.

---

### 💡 الفرق بين Friend Function و Friend Class:

| Friend Function | Friend Class |
| --- | --- |
| Function واحدة بس تبقى صاحبة | كلاس كامل يبقى صاحب |
| بنكتب `friend` قبل اسم الفانكشن | بنكتب `friend class B;` |
| ممكن تبقى global أو member | لازم تكون class كاملة |

---

### ❌ ليه ما نستخدمش الـ friend كتير؟

عشان بتكسر مبدأ مهم في OOP اسمه **Encapsulation**، المفروض الكلاس يحمي بياناته، لكن لما تفتحله الباب بـ `friend` بتكشف كل حاجة.

---

### 🧠 استخدامات مفيدة للـ friend:

- لو عايز تعمل **function operator overloading** (زي `<<` أو `>>`) وتدخل على data members خاصة.
- لو كلاس محتاج يدخل بيانات من كلاس تاني بدون ما تبني عليه inheritance.

---

```cpp
#include <iostream>
using namespace std;

// 1. إعلان مسبق عشان كلاس CRectangle يعرف إن في حاجة اسمها CSquare
class CSquare; 

class CRectangle {
    int width, height;
public:
    int area() { return (width * height); }
    // دالة هتحول المربع لمستطيل
    void convert(CSquare); 
};

class CSquare {
private:
    int side; // طول الضلع (private)
public:
    void set_side(int x) { side = x; }
    
    // 2. هنا بنقول إن CRectangle هو صديقنا (يقدر يشوف الـ side)
    friend class CRectangle; 
};

// 3. تعريف الدالة خارج الكلاس باستخدام Scope Resolution ::
void CRectangle::convert(CSquare a) {
    // لاحظ هنا: قدرنا نوصل لـ a.side مباشرة لأننا "أصدقاء"
    width = a.side;
    height = a.side;
}

int main() {
    CSquare sqr;
    CRectangle rect;
    
    sqr.set_side(4);    // ضلع المربع = 4
    rect.convert(sqr);  // حولنا المستطيل ليكون بنفس أبعاد المربع
    
    cout << rect.area() << endl; // النتيجة هتكون 16
    return 0;
}
```

### الـ `friend class CRectangle;`

بمجرد كتابة السطر ده جوه كلاس `CSquare`، إنت بتدي "تصريح أمني" لكلاس `CRectangle` إنه يدخل منطقة الـ `private` عندك ويستخدم متغير `side`.

### 2. دالة الـ `convert`

الدالة دي موجودة جوه `CRectangle`. هي بتاخد كائن من نوع `CSquare` وبتاخد قيمة الـ `side` بتاعته وتحطها في الـ `width` والـ `height` بتوع المستطيل. لولا كلمة `friend` كان السطر ده `width = a.side;` هيدي Error فوراً.

### 3. الترتيب مهم

لازم تعلن عن الـ `friend` جوه الكلاس اللي بياناته "محمية" (المربع في حالتنا).### ✅ الأول: يعني إيه Friend Function؟

ببساطة كدا:

هي **function** بنخليها "صاحبة" لكلاس معين، بحيث تقدر تدخل على **الـ private والـ protected data members** جوه الكلاس ده، بالرغم من إنها مش عضو (member) في الكلاس.

---

### 📌 الشكل العام:

```cpp
class MyClass {
private:
    int secret;

public:
    MyClass() { secret = 42; }

    // دي بقت صاحبة الكلاس
    friend void showSecret(MyClass obj);
};

void showSecret(MyClass obj) {
    cout << "Secret is: " << obj.secret << endl;  // مسموحلها تدخل
}

```

### ✅ ملحوظة:

- `friend` function مش لازم تكون جوه الكلاس، بس لازم يتكتب اسمها جواه بالكلمة المفتاحية `friend`.

---

### ✅ ثانيًا: يعني إيه Friend Class؟

دي بقى لما بنخلي **كلاس كامل** صاحب لكلاس تاني، يعني كل الـ functions اللي جوا الكلاس الصاحب يقدروا يدخلوا على الحاجات الخاصة (private/protected) بتاع الكلاس التاني.

---

### 📌 الشكل العام:

```cpp
class B;  // Forward declaration

class A {
private:
    int x;

public:
    A() { x = 100; }

    // بنخلي B صاحب A
    friend class B;
};

class B {
public:
    void printA(A obj) {
        cout << "Value of x: " << obj.x << endl;
    }
};

```

### ✅ ملحوظة:

- لما بنكتب `friend class B;` جوا `class A`، كل الفانكشنات بتاعة `B` تقدر تدخل على `A`.

---

### 💡 الفرق بين Friend Function و Friend Class:

| Friend Function | Friend Class |
| --- | --- |
| Function واحدة بس تبقى صاحبة | كلاس كامل يبقى صاحب |
| بنكتب `friend` قبل اسم الفانكشن | بنكتب `friend class B;` |
| ممكن تبقى global أو member | لازم تكون class كاملة |

---

### ❌ ليه ما نستخدمش الـ friend كتير؟

عشان بتكسر مبدأ مهم في OOP اسمه **Encapsulation**، المفروض الكلاس يحمي بياناته، لكن لما تفتحله الباب بـ `friend` بتكشف كل حاجة.

---

### 🧠 استخدامات مفيدة للـ friend:

- لو عايز تعمل **function operator overloading** (زي `<<` أو `>>`) وتدخل على data members خاصة.
- لو كلاس محتاج يدخل بيانات من كلاس تاني بدون ما تبني عليه inheritance.

---

```cpp
#include <iostream>
using namespace std;

// 1. إعلان مسبق عشان كلاس CRectangle يعرف إن في حاجة اسمها CSquare
class CSquare; 

class CRectangle {
    int width, height;
public:
    int area() { return (width * height); }
    // دالة هتحول المربع لمستطيل
    void convert(CSquare); 
};

class CSquare {
private:
    int side; // طول الضلع (private)
public:
    void set_side(int x) { side = x; }
    
    // 2. هنا بنقول إن CRectangle هو صديقنا (يقدر يشوف الـ side)
    friend class CRectangle; 
};

// 3. تعريف الدالة خارج الكلاس باستخدام Scope Resolution ::
void CRectangle::convert(CSquare a) {
    // لاحظ هنا: قدرنا نوصل لـ a.side مباشرة لأننا "أصدقاء"
    width = a.side;
    height = a.side;
}

int main() {
    CSquare sqr;
    CRectangle rect;
    
    sqr.set_side(4);    // ضلع المربع = 4
    rect.convert(sqr);  // حولنا المستطيل ليكون بنفس أبعاد المربع
    
    cout << rect.area() << endl; // النتيجة هتكون 16
    return 0;
}
```

### الـ `friend class CRectangle;`

بمجرد كتابة السطر ده جوه كلاس `CSquare`، إنت بتدي "تصريح أمني" لكلاس `CRectangle` إنه يدخل منطقة الـ `private` عندك ويستخدم متغير `side`.

### 2. دالة الـ `convert`

الدالة دي موجودة جوه `CRectangle`. هي بتاخد كائن من نوع `CSquare` وبتاخد قيمة الـ `side` بتاعته وتحطها في الـ `width` والـ `height` بتوع المستطيل. لولا كلمة `friend` كان السطر ده `width = a.side;` هيدي Error فوراً.

### 3. الترتيب مهم

لازم تعلن عن الـ `friend` جوه الكلاس اللي بياناته "محمية" (المربع في حالتنا).### ✅ الأول: يعني إيه Friend Function؟

ببساطة كدا:

هي **function** بنخليها "صاحبة" لكلاس معين، بحيث تقدر تدخل على **الـ private والـ protected data members** جوه الكلاس ده، بالرغم من إنها مش عضو (member) في الكلاس.

---

### 📌 الشكل العام:

```cpp
class MyClass {
private:
    int secret;

public:
    MyClass() { secret = 42; }

    // دي بقت صاحبة الكلاس
    friend void showSecret(MyClass obj);
};

void showSecret(MyClass obj) {
    cout << "Secret is: " << obj.secret << endl;  // مسموحلها تدخل
}

```

### ✅ ملحوظة:

- `friend` function مش لازم تكون جوه الكلاس، بس لازم يتكتب اسمها جواه بالكلمة المفتاحية `friend`.

---

### ✅ ثانيًا: يعني إيه Friend Class؟

دي بقى لما بنخلي **كلاس كامل** صاحب لكلاس تاني، يعني كل الـ functions اللي جوا الكلاس الصاحب يقدروا يدخلوا على الحاجات الخاصة (private/protected) بتاع الكلاس التاني.

---

### 📌 الشكل العام:

```cpp
class B;  // Forward declaration

class A {
private:
    int x;

public:
    A() { x = 100; }

    // بنخلي B صاحب A
    friend class B;
};

class B {
public:
    void printA(A obj) {
        cout << "Value of x: " << obj.x << endl;
    }
};

```

### ✅ ملحوظة:

- لما بنكتب `friend class B;` جوا `class A`، كل الفانكشنات بتاعة `B` تقدر تدخل على `A`.

---

### 💡 الفرق بين Friend Function و Friend Class:

| Friend Function | Friend Class |
| --- | --- |
| Function واحدة بس تبقى صاحبة | كلاس كامل يبقى صاحب |
| بنكتب `friend` قبل اسم الفانكشن | بنكتب `friend class B;` |
| ممكن تبقى global أو member | لازم تكون class كاملة |

---

### ❌ ليه ما نستخدمش الـ friend كتير؟

عشان بتكسر مبدأ مهم في OOP اسمه **Encapsulation**، المفروض الكلاس يحمي بياناته، لكن لما تفتحله الباب بـ `friend` بتكشف كل حاجة.

---

### 🧠 استخدامات مفيدة للـ friend:

- لو عايز تعمل **function operator overloading** (زي `<<` أو `>>`) وتدخل على data members خاصة.
- لو كلاس محتاج يدخل بيانات من كلاس تاني بدون ما تبني عليه inheritance.

---

```cpp
#include <iostream>
using namespace std;

// 1. إعلان مسبق عشان كلاس CRectangle يعرف إن في حاجة اسمها CSquare
class CSquare; 

class CRectangle {
    int width, height;
public:
    int area() { return (width * height); }
    // دالة هتحول المربع لمستطيل
    void convert(CSquare); 
};

class CSquare {
private:
    int side; // طول الضلع (private)
public:
    void set_side(int x) { side = x; }
    
    // 2. هنا بنقول إن CRectangle هو صديقنا (يقدر يشوف الـ side)
    friend class CRectangle; 
};

// 3. تعريف الدالة خارج الكلاس باستخدام Scope Resolution ::
void CRectangle::convert(CSquare a) {
    // لاحظ هنا: قدرنا نوصل لـ a.side مباشرة لأننا "أصدقاء"
    width = a.side;
    height = a.side;
}

int main() {
    CSquare sqr;
    CRectangle rect;
    
    sqr.set_side(4);    // ضلع المربع = 4
    rect.convert(sqr);  // حولنا المستطيل ليكون بنفس أبعاد المربع
    
    cout << rect.area() << endl; // النتيجة هتكون 16
    return 0;
}
```

### الـ `friend class CRectangle;`

بمجرد كتابة السطر ده جوه كلاس `CSquare`، إنت بتدي "تصريح أمني" لكلاس `CRectangle` إنه يدخل منطقة الـ `private` عندك ويستخدم متغير `side`.

### 2. دالة الـ `convert`

الدالة دي موجودة جوه `CRectangle`. هي بتاخد كائن من نوع `CSquare` وبتاخد قيمة الـ `side` بتاعته وتحطها في الـ `width` والـ `height` بتوع المستطيل. لولا كلمة `friend` كان السطر ده `width = a.side;` هيدي Error فوراً.

### 3. الترتيب مهم

لازم تعلن عن الـ `friend` جوه الكلاس اللي بياناته "محمية" (المربع في حالتنا).