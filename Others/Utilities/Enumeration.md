الطباعة: عند طباعة متغير من نوع الـ enum يتم طباعة القيمة الرقمية (Integer) المقابلة له.

بداية الترقيم: أول عنصر في الـ enum يبدأ تلقائيًا من القيمة 0.

تغيير البداية: يمكنك إجبار الـ enum على البدء من رقم معين (مثل sat = 1) وسيكمل هو العد تلقائياً من بعده.

الثبات: عناصر الـ enum تعتبر ثوابت؛ لا يمكن تعديل قيمها أثناء التشغيل، ولا يمكن للمستخدم إدخالها مباشرة كاسم.


---

## ✅ إيه هو الـ `enum`؟

كلمة **`enum`** معناها **تعداد**، وهي طريقة لتعريف مجموعة من القيم الثابتة لها أسماء مفهومة بدل الأرقام.

---

## 📌 الاستخدام:

تستخدم لما يكون عندك مجموعة خيارات محددة، زي أيام الأسبوع، ألوان، حالات، وهكذا.

---

## 🔹 الشكل العام:

```cpp
enum EnumName { VALUE1, VALUE2, VALUE3 };

```

---

## ✅ مثال بسيط:

```cpp
#include <iostream>
#include <string>
using namespace std;

// تعريف الـ enum وتحديد البداية من 1
enum days { sat = 1, sun, mon, tue, wed, thur, fri };

int main() {
// مصفوفة نصوص للطباعة
string d[7] = { "sat", "sun", "mon", "tue", "wed", "thur", "fri" };
// مصفوفة من نوع الـ enum
days m[7]   = { sat, sun, mon, tue, wed, thur, fri };

for (int i = 0; i < 7; i++) {
// m[i] تطبع الرقم ، d[i] تطبع النص
cout << m[i] << " - " << d[i] << endl;
}
}

```

### 

---

## ✅ القيم الرقمية:

الـ `enum` بيبدأ يعد من 0 تلقائيًا:

| الاسم | القيمة |
| --- | --- |
| SAT | 0 |
| SUN | 1 |
| MON | 2 |
| ... | ... |

ولو عايز تبدأ من رقم معين:

```cpp
enum Level { LOW = 1, MEDIUM = 5, HIGH = 10 };

```

---

## ✅ استخدام الـ enum داخل Class (Encapsulation)

```cpp
#include <iostream>
#include <string>
using namespace std;

string da[7] = { "sat", "sun", "mon", "tue", "wed", "thu", "fri" };
enum Days { sat = 1, sun, mon, tue, wed, thur, fri };

class Week {
private:
    Days d[7]; // مصفوفة خاصة من نوع التعداد
public:
    void setDay(Days w[]) {
        for (int i = 0; i < 7; i++) {
            d[i] = w[i];
        }
    }

    void print() {
        for (int i = 0; i < 7; i++) {
            // ربط اسم اليوم من المصفوفة الخارجية برقمه من المصفوفة الداخلية
            cout << "The number of day " << da[i] << " = " << d[i] << endl;
        }
    }
};

int main() {
    Days arr[7] = { sat, sun, mon, tue, wed, thur, fri };
    Week w;
    w.setDay(arr);
    w.print();
}

```

---

## ✅ ليه نستخدم `enum`؟

| السبب | الفايدة |
| --- | --- |
| 🧠 أوضح | بدل ما تكتب أرقام، تكتب اسم مفهوم |
| 🔒 أأمن | ميقبلش قيم غير المعرّفة |
| 🧹 منظم | يخلّي الكود منظم وقابل للتعديل |

---

## ✅ الفرق بين `enum` و `const int` أو `#define`

| الخاصية | `enum` | `#define` / `const int` |
| --- | --- | --- |
| تنظيم | منظم أكتر | مبعثر |
| التحكم | يمنع قيم غير مسموح بيها | ممكن تدخل أي رقم |
| القراءة | أسهل تقرأ الكود | ممكن تكون الأرقام غريبة المعنى |

---

## 🧠 لو عايز تستخدم `enum class` (نوع حديث من enum)

```cpp

enum class Color { RED, GREEN, BLUE };

Color c = Color::RED;

```

دي بتمنع التضارب بين enums مختلفة.