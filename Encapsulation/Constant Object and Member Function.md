**عند تعريف أوبجكت من نوع كونست لا يستطيع الوصول إلا للدوال التي من نوع كونست //**

**أما عند تعريف أوبجكت ليس من نوع كونست يستطيع الوصول جميع الدوال سواء من نوع كونست أو لا //**

---

## ✅ 1. `const object` – كائن ثابت

### يعني إيه؟

لو عملت object من كلاس عليه `const`، فده معناه إنك **مش هتقدر تعدل أي حاجة فيه** (ولا حتى بياناته، ولا تستدعي غير الدوال الثابتة `const functions`).

---

### ✅ مثال:

```cpp

#include <iostream>
using namespace std;

class Car {
public:
    int speed;

    void setSpeed(int s) { speed = s; }

    void show() const {
        cout << "Speed: " << speed << endl;
    }
};

int main() {
    const Car c1 ;  // كائن ثابت

    // c1.setSpeed(100); // ❌ خطأ: لا يمكنك تعديل كائن ثابت

    c1.show(); // ✅ مسموح لأن الدالة const
}

```

---

### ⚠️ ملاحظة:

- **ماينفعش** تستدعي أي دالة **غير** `const` مع كائن `const`.

---

## ✅ 2. `const member function` – دالة ثابتة

### يعني إيه؟

يعني إن الدالة **متوعدش تعدّل أي حاجة** في الكائن. لو حاولت تغيّر أي متغير عضو داخلها، الكومبايلر هيمنعك.

✅ تعريفها بيكون كده:

```cpp

void functionName() const;

```

---

### ✅ مثال:

```cpp
#include <iostream>
using namespace std;

class Car {
private:
    int speed;

public:
    Car(int s) { speed = s; }

    void show() const {
        cout << "Speed: " << speed << endl;

        // speed = 100; ❌ خطأ: لا يمكن التعديل داخل دالة const
    }
};

```

---

## ✅ مثال يجمع الاتنين:

```cpp

#include <iostream>
using namespace std;

class Student {
private:
    int grade;

public:
    Student(int g) { grade = g; }

    void show() const {
        cout << "Grade: " << grade << endl;
    }

    void setGrade(int g) {
        grade = g;
    }
};

int main() {
    const Student s1(90);
    s1.show();        // ✅ مسموح لأنها const
    // s1.setGrade(100); // ❌ خطأ لأن الكائن const

    Student s2(70);
    s2.setGrade(80);   // ✅ مسموح لأنه مش const
    s2.show();         // ✅
}

```

---

## 🔄 مقارنة بين const object و const function:

| المقارنة | `const object` | `const function` |
| --- | --- | --- |
| التعديل على البيانات | ❌ غير مسموح | ❌ داخل الدالة نفسها |
| استدعاء دوال عادية | ❌ غير مسموح | - |
| الاستخدام | تأمين الكائن من التعديل | تأمين الدالة من تغيير حالة الكائن |
| إجبار السلامة | يجبر المطور يكتب دوال ما تعدلش | يجبر المطور يحافظ على الكائن داخل الدالة |